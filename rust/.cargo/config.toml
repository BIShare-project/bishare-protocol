# Enable ARMv8 AES + PMULL hardware intrinsics in RustCrypto (aes 0.8.x / polyval 0.6.x
# gate them behind these cfgs; without them AES-256-GCM runs in software at ~1/11 the speed)
[target.'cfg(target_arch = "aarch64")']
rustflags = ["--cfg", "aes_armv8", "--cfg", "polyval_armv8"]
